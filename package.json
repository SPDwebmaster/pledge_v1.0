{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://spdwebmaster.github.io/pledge/",
  "dependencies": {
    "axios": "^0.22.0",
    "class-autobind": "^0.1.4",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-papaparse": "^3.17.2",
    "react-scripts": "^3.1.2"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "serve": "serve -s build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
